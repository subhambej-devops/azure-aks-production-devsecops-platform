apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ratings-api.fullname" . }}
  namespace: ratings
  labels:
    {{- include "ratings-api.labels" . | nindent 4 }}
  annotations:
    azure.workload.identity/client-id: {{ .Values.workloadIdentityClientId | quote }}

